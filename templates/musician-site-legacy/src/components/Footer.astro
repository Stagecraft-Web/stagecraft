---
import { getSiteConfig } from "../lib/content";

const site = getSiteConfig();
const socialEntries = Object.entries(site.socialLinks).filter(([, url]) => url);
// Auto-assembled so the year is always current and the boilerplate stays
// consistent across sites. Authors only customize the claimant name via
// `copyrightName`; it falls back to `artistName` when unset.
const copyrightHolder = site.copyrightName && site.copyrightName.length > 0
  ? site.copyrightName
  : site.artistName;
const copyrightLine = `© ${new Date().getFullYear()} ${copyrightHolder}. All rights reserved.`;
---

<footer class="site-footer">
  <div class="container footer-inner">
    {socialEntries.length > 0 && (
      <nav class="social-links" aria-label="Social media">
        {socialEntries.map(([platform, url]) => (
          <a href={url} target="_blank" rel="noopener noreferrer">
            {platform}
          </a>
        ))}
      </nav>
    )}
    <p class="copyright">{copyrightLine}</p>
  </div>
</footer>

<style>
  .site-footer {
    margin-top: auto;
    padding-block: 2rem;
    border-top: 1px solid var(--color-border);
    text-align: center;
  }

  .footer-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  .social-links {
    display: flex;
    gap: 1.5rem;
    flex-wrap: wrap;
    justify-content: center;
  }

  .social-links a {
    color: var(--color-text-muted);
    font-size: var(--font-size-sm);
    text-transform: capitalize;
  }

  .social-links a:hover {
    color: var(--color-secondary);
  }

  .copyright {
    font-size: var(--font-size-xs);
    color: var(--color-text-muted);
  }
</style>
